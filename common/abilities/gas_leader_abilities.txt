ability = {
	gas_attack = {
		name = "Spawn Gas On Offensive Combat Provinces"
		desc = ABILITY_FORCE_ATTACK_DESC
		
		sound_effect = command_power_ability_offensive
		
		type = army_leader
		
		allowed = {
			is_leading_army_group = no
			is_border_war = no
		}
		
		ai_will_do = {
			factor = -1
		}
		
		cost = 0
		duration = 1 #it's by hours. means nothing anyway
		allowed = {
			NOT = {
				has_trait = gas_attack
			}
			FROM = {
				has_war = yes
			}
		}
		one_time_effect = {
			set_temp_variable = { duration = 16 } #days
			
			meta_effect = {
				text = {
					add_timed_unit_leader_trait = {
						trait = gas_attack
						days = [DURATIONSTR]
					}
				}
				DURATIONSTR = duration
			}
			
			gas_attack_on_provinces = yes
			meta_effect = {
				text = {
					unit_leader_event = { 
						id = gas_unit_leader_events.[INDEX]
						hours = 1
					}
				}
				INDEX = THIS.indexed_id
			}
		}
	}
}